# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
data/
.env
server/__*.db*

# Relatório de cobertura — gerado por `npm run test:coverage`
coverage

# Exportações e backups do banco. NUNCA versionar: um dump carrega hash de senha, hash de token de
# sessão, e-mails e o dado de todas as empresas. Num repositório enviado ao GitHub — mesmo privado —
# isso é vazamento esperando acontecer.
backups/
*.sql.gz
jornada360-export-*.sql
